export function stringConcat(a: string, b: string) {
    return a + b;
}

/**
 * This function finds the sum of to numbers
 * @param a - first number
 * @param b - second numbers
 * @returns - sum of a and b
 */
export function myFunction(a: number, b: number) {
    return a + b;
}


